{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome",
            "url": "http://localhost:3001",
            "webRoot": "${workspaceFolder}/src",
            "skipFiles": ["<node_internals>/**", "/node_modules/**","/static/**"]
        }
    ]
}